[package]
name = "vsa-core"
description = "Local video clip/concat engine (FFmpeg subprocess)"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
